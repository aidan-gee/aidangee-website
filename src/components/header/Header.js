import React from 'react';
import './Header.css';

const Header = ({}) => (
    <nav>
        <ul>
            <li>
                Blog
            </li>
        </ul>
    </nav>
)

export default Header;
